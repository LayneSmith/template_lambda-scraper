import csv
import json
import requests

def scrapeStuff():
    # Assuming list of dicts for example
    # Doesn't have to be 'payload' just make sure it's a valid json object
    payload = []

    # Do all the things to fill the payload

    #  Return the payload
    return payload
